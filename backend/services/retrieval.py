class RetrievalService:
    """Hybrid retrieval over vector chunks plus exact procurement identifiers."""

    async def search(self, query: str, namespaces: list[str]) -> list[dict[str, object]]:
        return []

